server:
  port: 18010

spring:
  datasource:
    url: jdbc:mysql://database-1.c78kyz1h0q9p.us-east-2.rds.amazonaws.com:3306/testgcu?createDatabaseIfNotExist=true
    username: waganawa
    password: gachonackr3
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql # 추가 해준 부분
    #database-platform: org.hibernate.dialect.MySQL5InnoDBDialect # 추가 해준 부분
    show-sql: true

    hibernate:
      ddl-auto: update
    generate-ddl: true

    properties:
      dialect:
        #org.hibernate.dialect.MySQL5InnoDBDialect
      hibernate:
        format_sql: true

handlebars:
  suffix: .html
  expose-session-attribute: true
eureka:
  instance:
    hostname: nexus_drone_auth
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 30
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:18001/eureka/
